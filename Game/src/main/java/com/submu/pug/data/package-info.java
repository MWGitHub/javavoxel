/**
 * This package contains loaded data templates.
 * Treat the data in this package as immutable or else side effects may occur.
 * The data files should never be stored into a class member variable unless
 * the class is the one that created the data.
 */
package com.submu.pug.data;